import React from "react";

export default function Inventory() {
    return (
        <div className='inventory'>
            {/* TODO */}
            <p>This will require categories and displays for the images involved in my inventory. But it's
            hard to start without spring boot </p>
        </div>
    );
}